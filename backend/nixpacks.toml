[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["npm run build", "npm run migrate"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production" 